<%= form_with(model: [ @folder, @item ], local: true, multipart: true) do |form| %>
  
  <div class="field">
  
    <%= label_tag :attachment %>
    <%= form.file_field :attachment %>
  </div> 
  <div class="actions">
    <%= form.submit "Upload" %>
  </div>

<% end %>