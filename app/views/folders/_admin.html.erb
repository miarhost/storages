 <div class="row">
   <div class="col-sm-4">
    <h1>Folders</h1>
   </div>
   <div class="col-sm-4">
  <p class="text-gray">Folders</p>
  <p class="text-gray">User: <%= @user.email.split('@').first %></p>
  </div>
<table data-toggle="table" width = "100%" border="0" cellspacing="0" cellpadding="0">
 <thead style="background-color: none; font-family: 'Mark Pro'; font-size: 11px">
  <tr class="text gray">
   <th>Title</th>
   <th data-sortable="true">Description</th>
   <th>Items</th>
  </tr>
 </thead>

 <tbody>
  <% @folders.each do |folder| %>
   <tr>
   	<td><%= folder.title %></td>
    <td><%= folder.description %></td>
    <td><%= folder.items.count %></td>
   </tr>   
  <% end %>
 </tbody>
</table>